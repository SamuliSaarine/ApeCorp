# Creating a new agent

1. Create a new file in the `agents` directory
2. Import the `get_model` function from `utils.get_model`
3. Import the `Agent` class from `pydantic_ai`
4. Create a new agent instance using the `Agent` class and the `get_model` function